/**
 * Blank placeholder for the upcoming "New Targets" revenue targets experience.
 * Intentionally renders nothing — no header, cards, or copy — until built.
 */
export default function RevenueTargetsNew() {
  return null;
}
