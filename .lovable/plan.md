Replace the Download button's `onClick` handler in `src/components/procurement/ProductMasterTab.tsx` (around line 616) with the comprehensive export logic provided. The new handler maps `filtered` rows into a fixed shape including yield calculations (purchase, cooking, total yield, effective cost per base unit) and stock movement flag, then calls `downloadCSV` with the full 28-column schema regardless of currently visible table columns.

No other changes — UI, table columns, and surrounding logic remain untouched.