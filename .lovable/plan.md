
User reports sort functions not working properly across tables. Need to identify which table(s). Given recent work was on ForecastTableView, that's likely the culprit, but "tables where we have put sort functionality" suggests checking multiple. Let me investigate.
